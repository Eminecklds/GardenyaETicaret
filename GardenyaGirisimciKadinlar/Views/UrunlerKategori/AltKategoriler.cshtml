@model IEnumerable<GardenyaGirisimciKadinlar.Models.UrunlerGirisimciAltKatViewModel>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kategori.KategoriAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AltKategoriAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AltKategoriLink)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kategori.KategoriAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AltKategoriAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AltKategoriLink)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AltKategoriID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AltKategoriID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AltKategoriID })
        </td>
    </tr>
}

</table>
