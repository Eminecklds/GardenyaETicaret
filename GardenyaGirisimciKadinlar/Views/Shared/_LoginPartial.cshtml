@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right log">
            <li>
                @if(ViewBag.Resim!=null)
                { 
                <img src="~/Upload/Profil/@ViewBag.Resim" width="100" height="100" />
                }
            </li>
            <li>
                @if (true)
                {

                }
                @Html.ActionLink(User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
    }
}
else
{


    <div class="up-item">
        <i class="flaticon-profile"></i>
        @Html.ActionLink("Kullanıcı Giriş", "Login", "Account")
    </div>
    <div class="up-item">
        <i class="flaticon-profile"></i>@Html.ActionLink("Girişimci Giriş", "Login", "Account")
</div>    
    <div class="up-item" style="margin-left:20px;">
        <i class="flaticon-bag"></i>@{Html.RenderAction("CartSummary", "ShoppingCart");}
    </div>
    }
